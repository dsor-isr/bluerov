relay_server:
  # Video feed options
  stream_type: "HTTP" # Supports HTTP or UDP
  stream_port: 8080 # Port is not used when in HTTP mode
  stream_address: "0.0.0.0"

  # HTTP Broadcast options
  broadcast_port: 8080
  broadcast_address: "/"
  number_threads: 10
  image_quality: 100   # Ints from 0<->100 %
